import _modals from './modals'
import _currencyIcons from './currencies'

export const modals = _modals;
export const currencyIcons = _currencyIcons;

const constants = Object.freeze({
	modals,
	currencyIcons
})

export default constants